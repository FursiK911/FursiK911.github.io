import type { MetricPlacement } from '../types/projectCase.types'
export const metricPlacement: Record<string, MetricPlacement> = {
  mychessMetrics: { achievements: [0, 1, 2, 3, 4], facts: [] },
  mychessMobileMetrics: { achievements: [0, 1, 2, 3, 4], facts: [] },
  neo4SightlineMetrics: { achievements: [], facts: [0, 1, 2, 3] },
  virtualSommelierVillaKrimMetrics: { achievements: [0], facts: [1, 2, 3] },
  authorsWineVillaKrimMetrics: { achievements: [0], facts: [2, 3] },
  arColoringMetrics: { achievements: [2, 3], facts: [1] },
  chudobooksMetrics: { achievements: [2, 3], facts: [1] },
  arChudaboxesMetrics: { achievements: [0, 1, 2], facts: [] },
  earthDragonsMetrics: { achievements: [], facts: [0, 1, 2, 3] },
}
