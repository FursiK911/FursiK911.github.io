# 2026-08-31

## 19:44 +03:00 — Switched timeline runner to GSAP MotionPathPlugin

- Type: implementation
- Tags: ui, timeline, svg, gsap, animation, responsive, accessibility, testing, graphify
- Summary: implemented the reference-shaped SVG wave with pathProgress-driven milestone coordinates measured via `getTotalLength()`/`getPointAtLength()`, positioned cards below their curve points, and replaced native SVG motion with a looping GSAP MotionPathPlugin runner.
- Affected areas: `src/components/sections/ExperienceTimeline.tsx`, `src/components/sections/ExperienceTimelineItem.tsx`, `src/components/sections/FutureExperienceItem.tsx`, `src/App.css`, `src/i18n.ts`, `package.json`, `package-lock.json`, tests and Graphify artifacts.
- Rationale: follow the attached implementation plan and keep the timeline geometry, milestones, cards, and indicator on one responsive SVG path.
- Verification: targeted experience tests (11/11), typecheck, lint, production build, and incremental Graphify refresh passed; changed-file formatting passed, while the repository-wide format check still reports pre-existing style drift in `src/hooks/usePortraitGlitch.ts`, and full validation remains affected by the existing App/Hero visibility flake.

### Decision DEC-20260831-01

Use GSAP MotionPathPlugin for the continuous runner animation, guard it when SVG geometry APIs are unavailable in jsdom, and keep the reduced-motion path static.

## 19:59 +03:00 — Matched Hero portrait with the Glitch.js BROKEN HOLO demo

- Type: implementation
- Tags: ui, animation, glitch, accessibility, testing, debugging
- Summary: moved the Glitch.js target from the semantic portrait image to its wrapping container, restored the exact BROKEN HOLO effect stack, and removed the Hero-only photo filter that muted the hologram output.
- Affected areas: `src/hooks/usePortraitGlitch.ts`, `src/components/hero/GlitchPortrait.tsx`, `src/components/hero/GlitchPortrait.test.tsx`, `src/App.css`.
- Rationale: Glitch.js appends hologram and scanline overlays as children of its target; an `<img>` cannot render those children, unlike the demo's `<div>` target.
- Verification: focused GlitchPortrait tests passed (4/4); full validation reached coverage with one pre-existing App visibility timing flake, while the isolated App test passed (4/4).

### Decision DEC-20260831-02

Use the portrait wrapper as the Glitch.js target, keep the exact BROKEN HOLO configuration continuously active for visual comparison, allow demo-like overflow, and mark generated clones as decorative.

## 20:09 +03:00 — Unified the Experience timeline scene geometry

- Type: implementation
- Tags: ui, timeline, svg, gsap, animation, responsive, i18n, accessibility, testing, graphify
- Summary: rebuilt the desktop timeline as one responsive SVG scene, with every milestone positioned from the rendered curve via `getTotalLength()`, `getPointAtLength()` and `getScreenCTM()`; moved the green runner into a fixed-size HTML overlay; and replaced boxed cards with free-standing content below each point.
- Affected areas: `src/components/sections/ExperienceTimeline.tsx`, `src/components/sections/ExperienceTimelineItem.tsx`, `src/components/sections/FutureExperienceItem.tsx`, `src/data/workExperience.ts`, `src/i18n.ts`, `src/App.css`, experience tests, and Graphify artifacts.
- Rationale: the previous grid and SVG-circle implementation allowed points, cards, and runner to drift apart or distort. A shared scene geometry makes the reference-like wave stable at desktop widths while preserving the existing vertical mobile layout.
- Verification: focused experience tests (6/6), TypeScript, ESLint, and production build passed. Browser review at 1440 px confirmed that the dots sit on the path, cards begin below them, the runner remains circular, and the document has no horizontal overflow; 1024 px switched to the vertical layout without overflow. Incremental Graphify refresh completed. Repository-wide coverage remains blocked by two pre-existing App/Hero visibility timing failures.

### Decision DEC-20260831-03

Use a single measured SVG scene as the desktop source of truth for milestones and cards, while keeping the animated runner as a fixed-size HTML overlay so its circular shape cannot inherit non-uniform SVG scaling.

## 20:29 +03:00 — Added randomized short Glitch.js bursts

- Type: implementation
- Tags: ui, animation, glitch, timing, accessibility, testing
- Summary: kept the edited RGB/slice/shake effect parameters and changed Glitch.js to manual bursts with randomized 1–4 second pauses and 140–280 ms active windows.
- Affected areas: `src/hooks/usePortraitGlitch.ts`, `src/components/hero/GlitchPortrait.test.tsx`.
- Rationale: preserve the user's current glitch look while removing the continuous effect and making it appear as brief unpredictable portrait lag bursts.
- Verification: focused portrait tests (4/4), formatting, lint, typecheck, and production build passed.

### Decision DEC-20260831-04

Use manual Glitch.js bursts with a 1–4 second randomized pause and a 140–280 ms randomized burst duration; clear pause/burst timers and stop the active burst during cleanup.

## 20:32 +03:00 — Anchored evenly spaced Experience milestones to the SVG wave

- Type: implementation
- Tags: ui, timeline, svg, geometry, animation, responsive, testing, graphify
- Summary: replaced per-entry path progress with six equal horizontal target positions, each measured back onto the visible SVG wave by binary-searching its path length; moved Motion transforms from the positioned articles to their card content.
- Affected areas: `src/components/sections/ExperienceTimeline.tsx`, `src/components/sections/ExperienceTimelineGeometry.ts`, `src/components/sections/ExperienceTimelineItem.tsx`, `src/components/sections/FutureExperienceItem.tsx`, `src/data/workExperience.ts`, `src/App.css`, experience tests, and Graphify artifacts.
- Rationale: a Motion-managed transform on the positioned article displaced every milestone by half a card width. Uniform screen-space X targets make the points visually even while measuring their Y coordinates from the exact rendered curve.
- Verification: focused experience tests (7/7), formatting, ESLint, TypeScript, production build, browser geometry checks, and incremental Graphify refresh passed. At 1440 px the maximum measured point-to-path distance was 0.2 px, edge insets were 50 px, and horizontal intervals were 216 px; no horizontal overflow was observed from 1920 through 1025 px or on 1024/768/430/375 px mobile layouts. `npm run validate` reached coverage but remains blocked by the unrelated `GlitchPortrait` manual-burst timer assertion.

### Decision DEC-20260831-05

Use equal screen-space horizontal milestone positions with a fixed 50 px edge inset, then binary-search the monotonic SVG path for each corresponding Y coordinate.

## 20:55 +03:00 — Introduced shared action components

- Type: refactor
- Tags: ui, components, navigation, accessibility, testing, graphify
- Summary: introduced shared `ActionLink` and `ActionButton` components with primary, secondary, text, action, and inline variants; migrated CTA/navigation actions; and fixed Header and Contact primary-button hover conflicts.
- Affected areas: `src/components/ui/ActionLink.tsx`, `src/components/ui/ActionButton.tsx`, `src/components/ui/action.types.ts`, CTA consumers, `src/App.css`, tests, and Graphify artifacts.
- Rationale: centralize CTA behavior and prevent higher-specificity contextual link rules from changing primary button backgrounds or text colors.
- Verification: targeted shared/consumer tests passed (14 tests); formatting, lint, typecheck, and production build passed. Full coverage reached 67 passing tests with two pre-existing failures: the Hero visibility timing assertion in `src/app/App.test.tsx` and the stale Glitch.js parameter assertion in `src/components/hero/GlitchPortrait.test.tsx`. Incremental Graphify code-only refresh passed; non-code extraction remains unavailable without an LLM API key.

### Decision DEC-20260831-06

Use shared `ActionLink` and `ActionButton` components with explicit CTA variants to keep primary hover colors stable and prevent context-specific CSS conflicts.
