# Persistent project media mock

## 2026-09-06 18:40 +03:00

- Type: enhancement
- Tags: ui, projects, media, gallery, responsive, testing
- Summary: Made the cinematic project mock a persistent media frame for both previews and detail pages; images, YouTube previews, players, and technical placeholders now render inside its inset screen.
- Affected areas: `src/features/project-details/ui/ProjectMediaGallery` and its behavior tests.
- Rationale: Keep the left side visually consistent across projects while preventing real media from replacing the mock composition or touching the gallery edges.
- Verification: Four gallery behavior tests passed. ESLint, TypeScript, and the production build passed. Browser checks confirmed contained video playback, contained no-media placeholder content, detail-page rendering at `/projects/mychess-web`, and no horizontal overflow at `390×844`.

## Decision DEC-20260906-01

- Keep one persistent branded mock shell around all project media; expose actual images, video previews, players, and fallback copy only inside its inset screen in both preview and detail variants.

## 18:58 +03:00 — [feature] Transfer the loader portrait into the Hero scene

- **Type:** feature
- **Tags:** `ui`, `loading`, `animation`, `hero`, `hud`, `accessibility`, `responsive`, `testing`
- **Summary:** Replaced the loader slide-up with a staged portrait handoff: the completed scan holds briefly, the loader fades while a fixed clone preserves the photo, the clone transfers into the measured Hero image slot, and the Header, Hero, Directions, and HUD enter afterward.
- **Affected areas:** `src/features/loading`, `src/pages/home`, `src/widgets/profile`, `src/widgets/site-layout`, and behavior tests.
- **Rationale:** Preserve visual continuity from the identified profile in the loader to the portfolio portrait while preventing scroll and interaction before the first-screen reveal completes.
- **Verification:** Targeted loading, home, profile, and HUD tests passed (46 files, 46 tests); ESLint and TypeScript checks passed; browser checks completed at desktop and `390×844`. Full validation is rerun after the Graphify refresh.

## Decision DEC-20260906-02

- **Decision:** Treat the loader-to-Hero handoff as one explicit state machine: preserve only a fixed portrait clone during the `0.55s` loader fade, transfer it for `0.85s` to the live responsive Hero slot, reveal the page for `0.95s`, and unlock the document only after that reveal; skip uses a separate `0.25s` fade without a synthetic transfer.
- **Rationale:** Keeps animation completion, accessibility locking, timing controls, responsive geometry, and session persistence deterministic and testable.
